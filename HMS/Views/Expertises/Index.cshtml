@model IEnumerable<HMS.Models.Expertise>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Speciality)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Speciality)
            @Html.ActionLink(item.Speciality, "Find", new { degree = item.Speciality })
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExpertiseID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ExpertiseID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ExpertiseID })
        </td>
    </tr>
}

</table>
